# .env.example
# Mira Environment Configuration
# Copy to ~/.mira/.env and fill in your values
#
# Configuration is loaded once at startup from:
#   1. ~/.mira/.env (global)
#   2. .env (project-level, overrides global)

# ═══════════════════════════════════════════════════════════════════════════════
# API Keys (at least one LLM key recommended)
# ═══════════════════════════════════════════════════════════════════════════════

# DeepSeek - Primary LLM provider for experts and reasoning
# Get from: https://platform.deepseek.com/api_keys
DEEPSEEK_API_KEY=sk-your-deepseek-key-here

# Gemini - Required for semantic search (embeddings)
# Also usable as an LLM provider
# Get from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-key-here

# GLM 4.7 (Z.AI) - Alternative LLM provider
# Get from: https://open.bigmodel.cn/
# ZAI_API_KEY=your-zai-key-here

# ═══════════════════════════════════════════════════════════════════════════════
# LLM Provider Configuration
# ═══════════════════════════════════════════════════════════════════════════════

# Override default LLM provider (options: deepseek, gemini, glm)
# By default, Mira uses DeepSeek if available, then falls back to others
# DEFAULT_LLM_PROVIDER=deepseek

# For more fine-grained control, use ~/.mira/config.toml:
#   [llm]
#   expert_provider = "deepseek"      # For expert consultations
#   background_provider = "gemini"    # For summaries, briefings

# ═══════════════════════════════════════════════════════════════════════════════
# Embeddings Configuration
# ═══════════════════════════════════════════════════════════════════════════════

# Custom embedding dimensions (default: 768)
# Use 1536 for higher quality at the cost of more storage
# MIRA_EMBEDDING_DIMENSIONS=768

# Embedding task type (affects how embeddings are optimized)
# Options: SEMANTIC_SIMILARITY, RETRIEVAL_DOCUMENT, RETRIEVAL_QUERY,
#          CLASSIFICATION, CLUSTERING, CODE_RETRIEVAL_QUERY,
#          QUESTION_ANSWERING, FACT_VERIFICATION
# MIRA_EMBEDDING_TASK_TYPE=RETRIEVAL_DOCUMENT

# ═══════════════════════════════════════════════════════════════════════════════
# User Identity
# ═══════════════════════════════════════════════════════════════════════════════

# Override user identity for multi-user memory scoping
# By default, Mira uses git config user.email or system username
# MIRA_USER_ID=your-unique-id

# ═══════════════════════════════════════════════════════════════════════════════
# Web UI (not yet implemented)
# ═══════════════════════════════════════════════════════════════════════════════

# Custom port for web UI (default: 3000)
# MIRA_WEB_PORT=3000
