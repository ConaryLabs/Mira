[package]
name = "mira-types"
version = "0.1.0"
edition = "2024"
rust-version = "1.92"
description = "Shared types for Mira (native + WASM compatible)"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
